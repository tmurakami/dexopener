javadoc.options {
    bottom '<script>var l=document.links;for(var i=0;i<l.length;i++){var e=l[i];if(e.href.indexOf("http")===0)e.target="_blank"}</script>'
    addBooleanOption '-allow-script-in-comments', JavaVersion.current().java8Compatible && (System.properties['java.version'].replaceAll(/[^\d]/, '') as int) >= 180121
}
