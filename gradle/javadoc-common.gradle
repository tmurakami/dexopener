javadoc.options {
    bottom '<script>var l=document.links;for(var i=0;i<l.length;i++){var e=l[i];if(e.href.indexOf("http")===0)e.target="_blank"}</script>'
    def v = System.properties['java.version'] =~ /\d+/
    addBooleanOption '-allow-script-in-comments', v.size() == 4 && ((v[0] as int) > 1 || (v[1] as int) > 8 || (v[2] as int) > 0 || (v[3] as int) >= 121)
}
